{
  "name": "@immich-tag-browser/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "gen:api": "node scripts/gen-api.mjs"
  },
  "devDependencies": {
    "openapi-typescript": "^7.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
